<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PilTime" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="BasicButtonStyle">
        <item name="android:textColor">@color/black</item> <!-- 예: 텍스트 색상 빨강 -->
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:background">@drawable/background_rounded_corners</item><!-- 예: 배경색 초록 -->
    </style>

    <style name="Theme.PilTime" parent="Base.Theme.PilTime" />
</resources>