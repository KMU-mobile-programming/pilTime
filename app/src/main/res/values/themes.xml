<resources>

    <!-- 기본 테마 설정 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"> <!-- NoActionBar로 변경 -->
        <!-- 앱 전체의 기본 배경 색상 -->
        <item name="android:windowBackground">#F3D1A5</item>

        <!-- 버튼, 텍스트 색상 등 -->
        <item name="android:textColorPrimary">#000000</item> <!-- 텍스트 기본 색상 -->
        <item name="android:textColorSecondary">#333333</item> <!-- 보조 텍스트 색상 -->
    </style>
    <style name="Theme.PilTime" parent="Base.Theme.PilTime" />
</resources>
